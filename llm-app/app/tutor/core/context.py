"""Dead module — all context formatting has moved to context_middleware.py.

This file is intentionally empty. The formatting logic that was here
(compose_context, _format_question_context, etc.) now lives in
app.tutor.core.context_middleware and is injected via TutorContextMiddleware.
"""
